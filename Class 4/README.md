USEEFFECT & API FETCHING

UseEffect is a hook of react which is used to handle the side-effects of the webpage.

Api fetching means calling the server for the data or response.
1. fetch : in built
    It always returns us a non-readable data.
    So we have convert this into the json data.
2. axios : it is a library